---
const methods = [
  { name: 'Card', detail: 'Visa / Mastercard supported' },
  { name: 'PayPal', detail: 'Secure checkout' },
  { name: 'Local options', detail: 'Ask support for availability' },
];
---

<section class="relative py-24">
  <div class="relative z-10 max-w-7xl mx-auto px-6">
    <div class="grid lg:grid-cols-12 gap-12 items-start">
      <div class="lg:col-span-5 reveal">
        <div class="font-mono over">PAYMENTS</div>
        <h2 class="font-display font-black text-5xl md:text-6xl mt-4">
          Secure checkout.
          <br />
          <span style="color: var(--accent);">Clean start</span>.
        </h2>
        <p class="mt-6 text-lg" style="color: var(--muted);">
          Use a trusted payment method and we’ll guide activation end-to-end.
        </p>
      </div>

      <div class="lg:col-span-7 reveal" style="animation-delay:0.08s;">
        <div class="grid md:grid-cols-3 gap-6">
          {methods.map((m) => (
            <div class="card p-6">
              <div class="font-mono over">METHOD</div>
              <div class="mt-4 font-display font-bold text-2xl" style="color: rgba(255,255,255,0.84);">{m.name}</div>
              <p class="mt-3" style="color: var(--muted);">{m.detail}</p>
              <div class="mt-6 hairline"></div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .over {
    color: rgba(255,255,255,0.55);
    letter-spacing: 0.22em;
    text-transform: uppercase;
    font-size: 11px;
  }
</style>
